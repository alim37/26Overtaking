# ROS2 Controllers

This folder contains ROS2-side controller code kept fully inside `controller-benchmarks`.

Current package:

- `autodrive_f1tenth`: minimal Python ROS2 package containing a pure pursuit node that follows the ETHZ raceline scaled by `7.5x` for `f1tenth_1`.
